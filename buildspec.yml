version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Installation en cours..."
  build:
    commands:
      - echo "Build terminé avec succès !"

artifacts:
  files:
    - '**/*'
